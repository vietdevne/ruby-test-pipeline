#!/usr/bin/env bash
set -euo pipefail

source "$(dirname "$0")/env.sh"
require_command bundle

exec bundle exec ruby -e 'APP_PATH = File.expand_path("config/application", Dir.pwd); require "./config/boot"; require "rails/commands"' "$@"
