#!/usr/bin/env bash
set -euo pipefail

source "$(dirname "$0")/env.sh"
require_command bundle

exec bundle exec ruby -e 'require "./config/environment"; require "solid_queue/cli"; SolidQueue::Cli.start(ARGV)' "$@"
